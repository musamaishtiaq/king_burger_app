/// Shared preference keys used across settings and order flows.
class AppPrefs {
  AppPrefs._();

  /// When true, new orders default Home Delivery on (for a delivery device).
  static const String defaultHomeDeliveryEnabled = 'defaultHomeDeliveryEnabled';
}
