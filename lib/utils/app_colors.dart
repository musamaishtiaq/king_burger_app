import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Master palette (from provided UI reference)
  static const Color primary = Color(0xFFF07A2B); // warm orange
  static const Color background = Color(0xFFF6F6F6); // off-white
  static const Color surface = Colors.white;

  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF8B8B8B);

  static const Color border = Color(0xFFE8E8E8);
  static const Color disabled = Color(0xFFBDBDBD);
  static const Color error = Color(0xFFD32F2F);
}

